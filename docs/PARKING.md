# PARKING.md — Idées hors phase courante

Toute idée nouvelle non couverte par la phase en cours atterrit ici. Elle n'est ni rejetée ni oubliée.
Révision à chaque porte de sortie de phase.

| Date | Idée | Origine | Statut |
|---|---|---|---|
| 2026-07-03 | AASA / Universal Links (scheme baobab:// suffit pour TestFlight) | P0.5 audit | En attente Phase 1 |
| 2026-07-08 | Réintroduire expo-font (plugin app.json) quand les fonts custom du chantier design bible Jardin de Nuit seront implémentées | B4 triage | En attente chantier design |
| 2026-07-08 | `addPassObject` écrit le local et confirme l'UI avant le guard `createPassDelivery` — tout futur chemin d'envoi hors picker doit re-vérifier `canonicalRelationId` (cf. B6 : sans cette vérification, l'UI confirme un envoi que le counterpart ne reçoit jamais) | B6 triage | À vérifier sur tout nouveau point d'envoi de pass |
| 2026-07-09 | QR : montrer son QR et scanner en un geste (one-tap/swipe) — UX Phase 1 | Smoke test build 28 | En attente Phase 1 |
| 2026-07-10 | Scan : pré-remplir le champ nom depuis le `display_name` résolu (`lookupPublicProfile`) au lieu de forcer l'utilisateur à retaper (`app/relation/add.tsx:343` détecte `found` mais n'utilise pas le nom) | D-A(3) / B17 triage | En attente Phase 1 |
| 2026-07-12 | Picker dynamique + pass-as-signal — spec `docs/PASS-SIGNAL-SPEC.md` (boucle scores → suggestions → pass → évidence → scores). Phase 1 : ranking récence+catégorie (local) ; Phase 2 : évidence bayésienne. Décision Samo 12/07 | Décision produit | En attente Phase 1 |
| 2026-07-17 | Si rejet beta review ou pour la review App Store complète : implémenter un accès reviewer robuste (trigger test-email documenté ou endpoint de démo) — ne pas improviser sur `auth.users` en prod | Beta review kit (Test OTP email inexistant) | En attente review App Store |
| 2026-07-20 | Invite claimable par n'importe quel porteur du lien (pas de vérification d'identité au claim) — ajouter un écran de confirmation d'identité. Décision produit post-retours testeurs | Constat Sou 20/07 | En attente retours testeurs |
| 2026-07-21 | ~~Compléter `TIER_DISPLAY_FR` avec les mots de marque FR des tiers~~ → **Fait** : Enraciné/Pilier/Stable/Vivant/Naissant/Distant/Légende, micro-OTA `bb20f64` (update `019f81b4-d374-7822-87ea-ede00ab56663`) | B27-app (arbitrage C) | ✅ Clos 2026-07-21 |
| 2026-08-11 | **Migration de la taxonomie serveur des tiers** (Legend/Vibrant/Thrill/Spark/Ghost → échelle client actuelle Distant/Forming/Active/Steady/Anchor/Rooted) — le serveur émet encore l'ancienne taxonomie pré-V1 (`shared_reveal_day3_lifecycle.sql:175-186`, CHECK `day1:21`) ; sans impact tant que le client re-dérive le tier du score (B36-2 option A), mais **à traiter avant toute exposition publique des tiers**. | B36-2 (option A) | En attente, avant exposition publique |
| 2026-08-05 | **B33 — barre fantôme = artefact de rendu natif** (preuve sonde : `#1/1` rendu deux fois à l'écran, même instanceId, même y883 → une seule instance React, dupliquée par la couche native ; piste react-native-screens). Cosmétique, n'empêche aucun parcours. À revérifier sur le prochain build natif ; si persistant, investiguer react-native-screens / `enableScreens`. | B33 (sonde `fd48942` lue puis revert `8cd688b`) | À revérifier sur build 32 |
| 2026-08-05 | **B30-A — fusion complète atlas/liste** (garden absorbé dans l'accueil ; liste/carte en toggle d'un seul écran-maison « Jardin »). Non fait en B30 (Option B livrée : routage + barre partagée + vocabulaire). Chantier **design Jardin de Nuit** : chiffrage §5 de `docs/DIAG-B30.md` (~6-8 fichiers, risque **élevé** car touche l'écran de lancement, OTA-able) + **6 obstacles** §6 (param `filter` d'entrée, l'atlas s'approprie le viewport, double câblage resync B26, sélecteurs de store, garder `/(tabs)/garden` résolvable, retourner le vocabulaire « Carte »). Option B ne crée pas de dette pour A (même cible de nommage) | B30 (arbitrage : A parkée, B livrée) | En attente chantier design |
| 2026-07-22 | **B29-b — sync avatar via Supabase Storage** : bucket `avatars` + colonne `avatar_url` sur `user_public_profiles` + upload au save + exposition `counterpart_avatar_url` via `my_shared_relationships()` + rendu `<Image>` counterpart. **Décision produit post-Phase 0** : rompt la doctrine « no-PII » (avatars abstraits assumés, cf. commentaire `inviter_avatar_seed`), soulève privacy (visible par qui) + modération (aucun outil), lié au chantier design Jardin de Nuit. Chiffrage complet : `docs/DIAG-B29.md`. Note terrain : demande de Sou (photo mise, invisible côté Samo), 21/07. En attendant, a1 livré (label « visible par toi uniquement », OTA `7748324`) | B29 diagnostic (verdict local-only) | En attente post-Phase 0 |
| 2026-08-12 | **`first_viewed_at` serveur est global, pas par côté** : `shared_relationship_reveals` ne peut pas exprimer la doctrine « les DEUX ont lu » (`open_shared_reveal` estampille une seule fois, cf. cause racine B41). À corriger en **`first_viewed_at_side_a` / `_side_b`** (+ adapter `open_shared_reveal`). B41 contourne côté client (garde sur `firstViewedAt` LOCAL) — suffisant en Phase 0. **Ouvrir la table en même temps que la migration de taxonomie legacy des tiers (2026-08-11 ci-dessus) : une seule ouverture.** | B41 (cause racine serveur) | En attente, avec la migration taxonomie |
| 2026-08-12 | **Y5 — éligibilité au pass différée jusqu'à la cérémonie** : depuis B41, un `revealed` serveur non ouvert localement reste `reveal_ready`, donc l'éligibilité au pass n'est ouverte qu'après la cérémonie. **Changement de comportement produit** (assumé, cohérent avec « rien avant le reveal local ») — **à vérifier terrain** que ça ne bloque pas un parcours attendu. | B41 (test Y5 adapté) | À vérifier terrain |
| 2026-08-12 | **CTA « Préparation de la révélation… » a l'air désactivé** si l'appel serveur traîne : l'état de chargement du bouton d'ouverture de reveal ne se distingue pas assez d'un bouton grisé/inactif. UX à revoir (spinner/label explicite). | Constat terrain 12/08 | En attente Phase 1 |
| 2026-08-12 | **Jardin — un seul nœud affiché** alors que **13 relations existent** (12/08, capture 12:06). **Non diagnostiqué** : à instruire (filtre d'entrée ? hydratation store ? membres réseau non-`revealed` exclus, effet de bord B41 sur l'atlas ?). | Constat terrain 12/08 | À diagnostiquer |
| 2026-08-12 | **Logs `[B39]` à retirer** : ring buffer module + panneau appui-long + logs préfixés `[B39]`, à supprimer par **recherche du préfixe `[B39]`** quand le chantier reveal sera froid. ⚠️ **Garder** le tampon permanent `updateId`/`runtimeVersion` de l'écran Toi (utile en continu). | B39-ter (instrumentation temporaire) | À retirer chantier froid |
